#include <stdio.h>

/**
 * main - Entry point
 * main - Prints the numbers from 0 to 90
 *
 * Return: Always 0(Success)
 */
int main(void)
{



	for (int i = 0; i <= 9; i++)
	{
		printf("%d", i);
	}
	printf("\n");

	printf("0123456789\n");
return (0);
}
